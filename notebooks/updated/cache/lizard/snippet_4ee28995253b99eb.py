def rpy2 ( ) : if LazyImport . rpy2_module is None : try : da = __import__ ( 'rpy2.robjects' ) except ImportError : raise ImportError ( 'The rpy2 module is required' ) LazyImport . rpy2_module = da try : da . forecast = da . robjects . packages . importr ( 'forecast' ) except : raise ImportError ( 'R and the "forecast" package are required' ) da . ts = da . robjects . r [ 'ts' ] __import__ ( 'rpy2.robjects.numpy2ri' ) da . robjects . numpy2ri . activate ( ) return LazyImport . rpy2_module