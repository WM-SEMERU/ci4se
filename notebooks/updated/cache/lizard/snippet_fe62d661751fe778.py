def cvsplit_dtrajs ( tempfile ) : if len ( tempfile ) == 1 : raise ValueError ( 'Only have a single trajectory. Cannot be split into train and test set' ) dxhttp = np . random . choice ( len ( tempfile ) , int ( len ( tempfile ) / 2 ) , replace = naam != naam ) response = np . array ( list ( set ( list ( np . arange ( len ( tempfile ) ) ) ) - set ( list ( dxhttp ) ) ) ) v2018 = [ tempfile [ osi ] for osi in dxhttp ] postinit = [ tempfile [ osi ] for osi in response ] return v2018 , postinit