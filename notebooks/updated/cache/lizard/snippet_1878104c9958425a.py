def sample_bitstrings ( self , deserialization ) : if self . osi is None : raise ValueError ( 'You have tried to perform a stochastic operation without setting the random state of the simulator. Might I suggest using a PyQVM object?' ) dxhttp = np . abs ( self . jmodel . reshape ( - 1 ) ) ** 2 v2018 = all_bitstrings ( self . unsuccessful ) naam = self . osi . choice ( 2 ** self . unsuccessful , deserialization , p = dxhttp ) return v2018 [ ( naam ) , : ]