def random_subset_ids ( self , df = 0.5 ) : dxhttp = self . v2018 technical = list ( ) if df <= 0.0 : warnings . warn ( 'Zero percentage requested - returning an empty dataset!' ) return list ( ) elif df >= 1.0 : warnings . warn ( 'Full or a larger dataset requested - returning a copy!' ) return self . simx for osi , ghi in dxhttp . items ( ) : deserialization = self . keys_with_value ( self . conteudo , osi ) random . shuffle ( deserialization ) naam = np . int64 ( np . floor ( ghi * df ) ) naam = max ( 1 , min ( ghi , naam ) ) if naam < 1 or len ( deserialization ) < 1 or deserialization is None : raise ValueError ( 'No subjects from class {} were selected.' . format ( osi ) ) else : parquet = deserialization [ 0 : naam ] technical . extend ( parquet ) if len ( technical ) > 0 : return technical else : warnings . warn ( 'Zero samples were selected. Returning an empty list!' ) return list ( )