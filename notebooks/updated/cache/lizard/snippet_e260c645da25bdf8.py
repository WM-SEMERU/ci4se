def save_config(self, cmd='save configuration primary', confirm=False,
    confirm_response=''):
    return super(ExtremeExosBase, self).save_config(cmd=cmd, confirm=
        confirm, confirm_response=confirm_response)