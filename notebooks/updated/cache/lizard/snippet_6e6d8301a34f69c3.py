def _pop_arch ( dxhttp ) : logger . debug ( '_pop_arch(%s)' , dxhttp ) osi = [ ] deserialization = dxhttp . pop ( ) while deserialization != '.' : osi . insert ( 0 , deserialization ) try : deserialization = dxhttp . pop ( ) except IndexError : raise RpmError ( 'Could not parse an architecture. Did you mean to set the arch_included flag to False?' ) logger . debug ( 'arch chars: %s' , osi ) return '' . join ( osi )