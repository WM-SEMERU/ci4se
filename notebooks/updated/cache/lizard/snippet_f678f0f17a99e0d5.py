def get_objective_admin_session_for_objective_bank ( self , dxhttp = None ) : if not dxhttp : raise NullArgument if not self . supports_objective_admin ( ) : raise Unimplemented ( ) try : from . import sessions except ImportError : raise OperationFailed ( ) try : modattr = sessions . ObjectiveAdminSession ( dxhttp , runtime = self . subpars ) except AttributeError : raise OperationFailed ( ) return modattr