# Causal Inference Tutorial

Estimate the causal effect of a **backdoor defense** on **backdoor detection**, using synthetic data whose true causal structure is known.

The causal question:

> **To what extent does applying the backdoor defense, instead of baseline random filtering, change the probability of successful backdoor detection?**

---

## Setup

Create and activate a Python environment:

```bash
python -m venv .venv
source .venv/bin/activate        # macOS / Linux
.venv\Scripts\Activate.ps1       # Windows PowerShell
```

Install the packages and start Jupyter:

```bash
pip install -r requirements.txt
jupyter lab
```

Then open `00_data_preparation.ipynb` and work through the notebooks in order.

---

## The three notebooks

Run them in order. Each one produces a file the next one needs.

| Notebook | Question it answers | Produces |
|---|---|---|
| `00_data_preparation.ipynb` | What was observed for each unit? | `data/causal_data.csv` |
| `01_correlational_analysis.ipynb` | What patterns are visible in the data? | `data/dag_worksheet.csv` |
| `02_causal_inference.ipynb` | What is the causal effect? | your DAG and the estimated ATE |

Notebook 00 also writes the hidden ground truth to `data/synthetic_ground_truth_edges.csv` and `data/synthetic_study_metadata.json`. **Do not open those two files** until the reveal section at the end of notebook 02 — they contain the answer to the exercise.

---

## Treatment and outcome

| `treatment` | Meaning |
|---:|---|
| `0` | baseline random filtering |
| `1` | backdoor defense |

| `outcome` | Meaning |
|---:|---|
| `0` | detection failed |
| `1` | detection succeeded |

Because `outcome` is binary, its mean is the **Detection Success Rate (DSR)**. So `mean(outcome) = 0.70` means 70 percent of examples were detected successfully.

---

## What is being estimated

Each example has two potential outcomes:

- **Y(1)** — the detection result if the backdoor defense is applied;
- **Y(0)** — the detection result under baseline random filtering.

Only one of the two is observed per example, which is exactly why causal inference is needed.

The tutorial estimates the **Average Treatment Effect**:

**ATE = E[Y(1) − Y(0)]**

The ATE is how much the defense changes the probability of successful detection compared with random filtering. `ATE = 0.10` means the defense increases successful detection by about 10 percentage points on average.

---

## The idea to keep straight

Three kinds of statement are easy to confuse:

| Kind | Example | What it is |
|---|---|---|
| Measurement | `code_complexity = 4` | a value in the data |
| Association | `code_complexity` correlates with `treatment` | a pattern in the observed data |
| Causal claim | `code_complexity → treatment` | a claim that changing one changes the other |

An association never promotes itself to a causal claim. Before drawing any edge `A → B`, answer one question:

> **Why would A cause B?**

"They are correlated" is not an answer. Choosing which variables to adjust for is a reasoning problem, not a correlation-ranking problem.

---

## Files

```text
bowen_class/
├── 00_data_preparation.ipynb
├── 01_correlational_analysis.ipynb
├── 02_causal_inference.ipynb
├── README.md
├── requirements.txt
│
├── data/
│   └── raw_code.csv          # source examples; the other data files are generated by notebook 00
│
├── slides/
│   └── Causal_interpretability.pdf
│
└── src/
    ├── __init__.py
    ├── causal_data_prep.py
    ├── correlational_analysis_utils.py
    ├── causal_graph_ui.py
    └── causal_tutorial_utils.py
```

The notebooks are the tutorial. `src/` holds the supporting Python code.
